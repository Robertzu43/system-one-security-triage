{
  "name": "system-one-security-triage",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test --test-concurrency=1 dist/src/*.test.js",
    "demo": "npm run build --silent && node dist/src/cli.js demo --fixture test/fixtures/demo-cases.json",
    "demo:live": "npm run build --silent && node dist/src/cli.js demo --live --fixture test/fixtures/demo-cases.json",
    "triage": "npm run build --silent && node dist/src/cli.js",
    "test:e2e": "npm run build --silent && node dist/src/cli.js run --fixture test/fixtures/repository/candidates --evaluator fixture",
    "check": "npm test && git diff --check"
  },
  "dependencies": {
    "@typesafe-ai/sdk": "0.6.0"
  },
  "devDependencies": {
    "@types/node": "24.5.2",
    "typescript": "5.9.2"
  }
}
